import React from 'react';
import Header from '../Header/Header';

const SignIn = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is sign in components</h1>
        </div>
    );
};

export default SignIn;